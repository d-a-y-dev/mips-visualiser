all: test

test: 32_text_input_and_clipboard_handling.cpp
	g++ -g -O2 -DDEBUG_FLAG $^ -o $@ -I SDL2/include -L SDL2/lib -l SDL2-2.0.0 -L SDL2_ttf/lib -l SDL2_ttf-2.0.0  -L SDL2_image/lib -l SDL2_image-2.0.0

.PHONY: clean
clean:
	rm -rf *.o *~ test
